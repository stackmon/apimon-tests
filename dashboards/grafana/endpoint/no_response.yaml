---
order: 2
gridPos:
  w: 8
  h: 9
  x: 16
  y: 2
bars: true
fieldConfig:
  defaults:
    custom:
      drawStyle: "bars"
      fillOpacity: 10
      spanNulls: 3600000
      showPoints: "never"
options:
  legend: {displayMode: "list", placement: "bottom"}
  tooltip: {mode: "single", sort: "none"}
targets:
  - refId: "A"
    target: "groupByNode(removeBelowValue(stats.counters.openstack.api.$environment.$zone.*.*.*.failed.count, 0.1), 6, 'sum')"
title: "No response"
type: "timeseries"
